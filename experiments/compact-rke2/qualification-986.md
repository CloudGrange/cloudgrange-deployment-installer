# Runtime persistence and cached resume qualification

The deployed installer remains `8819c5fd808ad5bca9dbeddc70cc6d36773dd5a3`, with its original config, artifact manifest and installed request. The infrastructure owner supplies independent probes; the installed request guard is not bypassed. These observations advance AB#8914/AB#8915 without closing the full parent acceptance.

Reboot983 succeeded: configuration, runtime binary/checkpoint, data-volume identity and disk/Kubernetes markers survived. SSH returned on a changed boot ID after111.43seconds; total verification completed after222.96seconds. Network984 then passed fresh pod DNS, service HTTP and configured/wrong-host ingress checks on that new boot. These are runtime observations, not application backup/restore qualification.

[Egress986](https://dev.azure.com/hybridcloudsolutions/CloudGrange/_build/results?buildId=986) completed Succeeded on infrastructure `36da61fba79b3230c298f60acf0b051c353d69b8`. It verified the previously failed985 service/node had recovered, cancelled its timer and archived its guard while preserving its exact failed result. The original installer Plan verified the cached archives. The probe stopped the RKE2 service, invoked the original Install resume, and waited until systemd reported active while public egress remained denied. The original node identity and immutable ConfigMap marker survived. Both direct public TCP controls failed before/after bootstrap and connected after native host policy restoration. Private domain DNS remained available. The Linux recovery timer was cancelled after active-state verification.

Original986 artifact ZIP SHA256: `4569ed78b0dcb2a0426dbd2d13b9c8bc572756e0d517bb5861be99e32dadb8b0`. Its private evidence capsule is851221bytes, SHA256 `ec1eac5b23409d77be4e4b361dce40abf5a1cb87e0dfbb00245faa7456f90822`, blob version `2026-09-11T02:55:47.3815414Z`. The pipeline rejected duplicate upload403 and verified exact-version readback, original file bytes and bound source/execution identities.

Run985 remains Failed. Existing Kubernetes processes answered NodeReady while the newly started RKE2 service was still activating. The first probe checked systemd too soon; its original ZIP `acb2d742219a8941bb3d27c45fedb5e2afe6e0fba74c43b2fdd36592583f3f35` is retained. The corrected probe adds bounded active-state observation. An existing NodeReady response alone is insufficient evidence that a restarted runtime service has completed startup.

To reproduce through the existing tppoc pipeline, select PrepareCompactRuntime/EgressProbe, pin the reviewed infrastructure and original installer commits, and use a distinct execution. Existing experiment state prevents implicit replay. A corrected attempt requires explicit prior execution/result digest and observed host/service recovery; a successful experiment is not automatically admitted for repetition. See the infrastructure owner's `docs/tppoc-egress-probe.md` and actual pipeline parameters for the original986 invocation.

Limits: this is cached resume of an existing node, not fresh-node offline bootstrap or disconnected-distribution certification. Existing container processes may remain running when the RKE2 service stops. Full interruption/partial-bootstrap recovery, sustained resource footprint, product storage and backup/recovery, teardown/promotion into F12-1, TLS/PKI, the assembled product and all original M0/M1 requirements remain. Neither requested Hyper-V cluster exists yet.

Subsequent host cleanup verification: direct full-path diagnostic and native retry cg-lab-inspect-egress-986-retry1 both passed, observing zero ACLs/recovery tasks and no temporary SSH key or matching SSH processes. The Windows host reports PS7.4.20; its bootstrap pin/configuration were reconciled and restored by a successful one-host deployment. The earlier handler failure remains retained with unproven cause. Linux/runtime input pins are unchanged.
