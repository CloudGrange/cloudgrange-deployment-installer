apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
  labels:
    app.kubernetes.io/name: cloudgrange-api
spec:
  selector:
    app.kubernetes.io/name: cloudgrange-api
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
