apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-portal
  labels:
    app.kubernetes.io/name: cloudgrange-portal
spec:
  selector:
    app.kubernetes.io/name: cloudgrange-portal
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
