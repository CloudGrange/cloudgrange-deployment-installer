global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: cloudsmith-api
    static_configs:
      - targets: ['cloudsmith-api:8080']
    metrics_path: /metrics
    scheme: http
