global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: cloudsmith-api
    static_configs:
      - targets: ['cloudsmith-api:9090']
    metrics_path: /metrics
    scheme: https
    tls_config:
      insecure_skip_verify: true
